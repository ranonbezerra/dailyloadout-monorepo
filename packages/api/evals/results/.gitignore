# The latest run is a transient, per-run artifact (recaps regenerate each run).
# Only the promoted/saved baseline.json is committed.
latest.json

# Per-machine gate fingerprint (memoizes the last passing content). Not shared.
.gate-cache
